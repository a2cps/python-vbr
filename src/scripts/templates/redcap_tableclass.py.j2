"""{{ module_docstring }}
"""

from ....pgrest import *
from ...constants import Constants
from ..rcconstants import REDCapConstants

from ..rcaptable import RcapTable

__all__ = ['{{class_name}}']

class {{class_name}}(RcapTable):
    """{{docstring}}
    """
    __redcap_form_name = '{{ redcap_form_name }}'
    {{redcap_form_name}}_id = Constants.SERIAL_PRIMARY_KEY_COLUMN
    {{redcap_form_name}}_complete = Column(Integer, ForeignKey('status.status_id'))
    {%- for k, v in columns.items() %}
    {% for cc in v.comments %}# {{ cc }}
    {% endfor %}{{ k }} = Column({{v.type}}, nullable={{ v.nullable }}, comments=None){% endfor %}
